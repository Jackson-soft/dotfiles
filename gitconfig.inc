[core]
    editor = nvim
    pager = delta

[pull]
    rebase = false

[interactive]
    diffFilter = delta --diff-so-fancy

[delta]
    line-numbers = true
    navigate = true    # use n and N to move between diff sections

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
